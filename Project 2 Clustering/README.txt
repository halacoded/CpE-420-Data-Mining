CpE 420: Data Mining - Project 2: Clustering

Student: Hala Almutairi     ID: 2211112873


## Note

The Colab notebook is where you run everything.

The Python script (`cluster_analysis.py`) contains the clustering algorithms and required functions for the programming tasks.

You do not need to manually edit the `.py` file during execution; the Colab notebook calls it automatically.


## Program Files (Two Main Files)

### Project2.ipynb
Main Colab notebook where the program is executed.  
Contains the workflow for:

- Loading datasets
- Running clustering algorithms
- Evaluating clustering performance
- Generating Silhouette Coefficient plots

### cluster_analysis.py
Python script containing the core clustering functions for:

- K-Means Clustering
- K-Medoids Clustering

This file is executed from the Colab notebook.


## Dataset Files

The project uses the following datasets:

- `AllBooks_baseline_DTM_Unlabelled.csv`
- `AllBooks_baseline_DTM_Labelled.csv`

The unlabelled dataset is used for clustering.

The labelled dataset may be used for analysis and report comments.

---

## View Results Only (No Need to Run)

The notebook has already been executed once.

To view the outputs:

1. Open `Project2.ipynb` in Google Colab
2. Scroll through the notebook cells
3. The notebook is divided according to project tasks
4. Outputs, clustering labels, Silhouette Coefficient results, and plots are already displayed

---

## How to Run the Program (If Needed)

### Step 1
Upload the project folder to Google Drive.

### Step 2
Open `Project2.ipynb` in Google Colab.

### Step 3
Connect Google Drive to the notebook.

### Step 4
Run all notebook cells in order.
